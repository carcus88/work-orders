import React from 'react';

function AddItem(props) {
    return (
        <div className="form-group">
            <button className="form-control btn btn-primary">Add Item</button>
        </div>
    );
}

export default AddItem;